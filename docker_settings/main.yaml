version: "3"
networks:
  network:
    driver: bridge
services:
  # mysql
  import: docker_settings/services/mysql/mysql.yaml
  import: docker_settings/services/rust/rust.yaml
  import: docker_settings/services/phpmyadmin/phpmyadmin.yaml
